//用于存放第七章的c primer plus 的题目
#include<stdio.h>
int main()
{
	int num = 6;
	int i, j;
	int cnt = 0;   //这行代码的位置决定整个程序正确与否
	for (i = 0; i < num; i++) {
		//如果int cnt =0;在这的话，每次进入第二个循环时，cnt都被初始化为0，而在外面cnt不断被改变
		//所以如果期望cnt不断改变就放循环外面，cnt不断累加而不会随着循环一起被初始化为0
		
		
		for (j = 0; j <= i; j++) {
			printf("%c", 'A'+cnt);
			cnt++;
		}
		printf("\n");
	}
  return 0;
  }
