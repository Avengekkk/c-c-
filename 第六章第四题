//用于存放第七章的c primer plus 的题目
#include<stdio.h>
int main()
{
	int num = 6;
	int i, j;
	int cnt = 0;   //这行代码的位置决定整个程序正确与否
	for (i = 0; i < num; i++) {
		
		for (j = 0; j <= i; j++) {
			printf("%c", 'A'+cnt);
			cnt++;
		}
		printf("\n");
	}
  return 0;
  }
